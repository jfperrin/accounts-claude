services:
  app:
    build: .
    ports:
      - "80:3001"
    env_file: ./server/.env
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO /dev/null http://localhost:3001/api/auth/config"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
